# include "square_calculation.h"

double CalcSquare(double width, double height) {
// double CalcSquare(Wall wall) {
    return width * height;
   // return wall.GetHeight()*wall.GetWidth();
}
